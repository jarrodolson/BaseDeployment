# Readme file or base deployment

This package contains the key tools used or most data collection activities.

# To Install, install python 3.5, add a virtual environment, and then run:

```
pip install -r requirements.txt
```

Install your database engine of choice and its driver, in this case psycopg2

```
python
